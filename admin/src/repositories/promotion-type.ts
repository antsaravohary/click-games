
import { CreatePromotionType } from "@ts-types/promotion-type.types";
import Base from "./base";

class PromotionType extends Base<CreatePromotionType, null> {
	
}

export default new PromotionType();
